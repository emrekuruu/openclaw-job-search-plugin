{
  "id": "job-search",
  "name": "Job Search",
  "description": "Skill-first job-search package. The plugin entrypoint is intentionally minimal; workflow capabilities live under bundled skills, scripts, and references.",
  "skills": [
    "skills/environment-check",
    "skills/job-search-skill",
    "skills/job-listing-evaluation-skill",
    "skills/job-resume-generation-skill"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "evaluationConcurrency": {
        "type": "integer",
        "minimum": 1,
        "description": "Maximum concurrent evaluator subagent runs."
      },
      "evaluationModel": {
        "type": "string",
        "description": "Optional canonical provider/model override for evaluator subagents."
      },
      "defaultResumeTheme": {
        "type": "string",
        "description": "JSON Resume theme package name to use by default when rendering resumes. Use a working installed theme package such as jsonresume-theme-even unless a local custom theme is wired in."
      }
    }
  }
}
