[project]
name = "job-search-bot"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pandas",
    "pydantic",
    "python-jobspy",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
