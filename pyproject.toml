[project]
name = "job-search-bot"
version = "0.1.0"
description = "OpenClaw job-search plugin with Python JobSpy retrieval worker"
requires-python = ">=3.11"
dependencies = [
  "python-jobspy"
]

[tool.uv]
package = false
